
# Shared secret the biometric punch terminal presents when pushing events to
# /api/attendance/hook. Unset = the hook is closed. 8-16 chars: the Hikvision
# firmware will not accept a longer password.
ATTENDANCE_HOOK_SECRET="generate-8-to-16-chars"
